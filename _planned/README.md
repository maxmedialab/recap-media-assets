# _planned/ — drafted pages, NOT live

**Convention:** `_planned/` holds pages that are drafted or approved-in-progress but **not live** — the copy is still moving through Max's approval, so nothing here gets pasted into GHL yet. Contrast with `_parked/`, which holds pages that are **built and final but gated** on an external condition (e.g. `_parked/livestreaming.html`, gated on the livestream partner viability gates).

Same local-preview convention as `_parked/`: pages reference `../css/style.css` and `../js/main-v2.js`.

## Pages

| File | Copy doc (source of truth) | Status |
|------|----------------------------|--------|
| `index-icp-v2.html` | `projects/recap-website-cleanup/icp-homepage-copy-v2.md` | Awaiting Max's copy approval (hero sub-line A/B, non-profit line A/B/C, voice pass on the new sections) |

## Where the bigger picture lives

The live/planned split for the whole site restructure is documented in `projects/recap-website-cleanup/site-restructure-plan.md`.
